{
  "build": {
    "env": {
      "NODE_OPTIONS": "--openssl-legacy-provider"
    },
    "command": "npm install -g hexo-cli && npm install && hexo clean && hexo generate",
    "output": "public"
  },
  "routes": [{ "src": "/(.*)", "dest": "/$1" }],
  "framework": null
}